import { http, HttpResponse } from 'msw'

// Auto-registered by handlers/index.ts (import.meta.glob) — no edits to a shared file (plan §5).
export default [http.get('/api/{{kebabCase name}}', () => HttpResponse.json([]))]
