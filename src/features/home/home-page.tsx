/**
 * Home route. Phase 1 placeholder — the acceptance increment turns this into the demo that
 * renders a Zod-validated form and a query resolving against the MSW backend.
 */
export const HomePage = () => (
  <div className="space-y-2">
    <h1 className="text-2xl font-semibold">Welcome</h1>
    <p className="text-muted-foreground">Phase 1 routing is live.</p>
  </div>
)
